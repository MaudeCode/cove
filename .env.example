# Cove Environment Configuration
# Copy to .env.local for local overrides (gitignored)

# Log level: debug | info | warn | error | off
# Default: "off" in production, "warn" in dev
VITE_LOG_LEVEL=debug
